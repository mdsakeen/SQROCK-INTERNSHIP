# Day 12 - Phishing Email Detection with ML

Train a Naive Bayes classifier on 50 synthetic labeled email samples.

Install: `pip install -r requirements.txt`
Run: `python src/train_model.py`

Deliverable: trained model, confusion matrix, and measured accuracy report on a 50-sample dataset.
